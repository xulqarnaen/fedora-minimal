-- Input
hl.config({
    input = {
        kb_layout = "us",
        follow_mouse = 1,
        sensitivity = 0,
    },

    cursor = {
        hide_on_key_press = true,
        warp_on_change_workspace = 0,
    },
})
